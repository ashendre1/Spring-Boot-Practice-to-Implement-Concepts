server:
  port: 8082

spring:
  application:
    name: order-service

  r2dbc:
    url: r2dbc:postgresql://localhost:5432/orderdb
    username: postgres
    password: aniket1999

  sql:
    init:
      mode: always

# Product service URL for inter-service communication
services:
  product:
    url: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  endpoint:
    health:
      show-details: always

logging:
  level:
    com.practice.order: DEBUG

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

